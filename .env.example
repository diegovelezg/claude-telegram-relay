# ============================================================
# Discord + Telegram Relay Configuration
# ============================================================

# --- Required: At least one platform ---

# Telegram Bot Token (get from @BotFather)
TELEGRAM_BOT_TOKEN=
# Telegram User ID (get from @userinfobot) - leave empty for any user
TELEGRAM_USER_ID=

# Discord Bot Token (get from Discord Developer Portal)
DISCORD_BOT_TOKEN=
# Discord Channel IDs (comma-separated) - leave empty for all channels
DISCORD_CHANNEL_IDS=


# --- MCP Configuration (required for memory) ---

# MCP Server URL (Diego's MCP)
MCP_URL=http://localhost:3001/sse
# MCP API Key
MCP_API_KEY=


# --- Claude Configuration ---

# Path to Claude CLI (default: "claude")
CLAUDE_PATH=claude
# Working directory for Claude (default: relay directory)
PROJECT_DIR=


# --- User Configuration ---

# Your name (for context)
USER_NAME=Diego
# Your timezone
USER_TIMEZONE=America/Lima


# --- Optional: Voice Transcription ---

# Voice provider: "groq" or "local"
# VOICE_PROVIDER=groq
# Groq API key (free at console.groq.com)
# GROQ_API_KEY=


# --- Optional: Directories ---

# Relay working directory (default: ~/.discord-telegram-relay)
# RELAY_DIR=


# ============================================================
# Security Notes for VPS Deployment:
# ============================================================
# 1. Use environment variables, not .env file on production
# 2. Run behind a reverse proxy (nginx) with SSL
# 3. Use systemd service for auto-restart
# 4. Consider fail2ban for brute-force protection
# 5. Keep bot tokens in /etc/environment or systemd units
